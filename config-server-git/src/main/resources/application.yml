spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fancuodexiatian/lxqProject
#          search-paths: repo
#          username:
#          password:

server:
  port: 1201